# The third specimen of the basic system for TEG RL calculations

The content of `scripts/rl_model.py`, its common logic, the algorithm of its calculations and the construction of TEGs calculated by it are subject to copyright.
The copyright for the content of `rl_model.py` is confirmed by a certificate (`Certificate.pdf`).
The copyright for the construction of TEGs designed by `rl_model.py` is confirmed by a patent (`Patent.pdf`).
